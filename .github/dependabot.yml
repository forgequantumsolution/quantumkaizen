# Dependabot disabled. Previous config opened 20+ PRs per week and they
# sat unmerged. Re-enable by restoring an earlier version from git history
# and changing `directory: /server` to `/backend` (folder was renamed).

version: 2
updates: []
